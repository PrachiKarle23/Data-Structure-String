string reverse(string s){
	int i=0,n=s.length()-1;
	while(i<=n){
		swap(s[i],s[n]);
		i++;
		n--;
	}
	return s;
}
bool isPalindrome(string &s)
{
	string p=reverse(s);
    for(int i=0;i<s.length();i++){
        if(s[i]!=p[i]){
         return false;
         }
    }
    return true;
}
